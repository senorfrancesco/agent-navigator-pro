apiVersion: 1

providers:
  - name: AgentNavigator
    orgId: 1
    folder: Agent Navigator
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
