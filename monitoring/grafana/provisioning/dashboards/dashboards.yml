apiVersion: 1

providers:
  - name: llm-tools-platform
    orgId: 1
    folder: llm-tools-platform
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
