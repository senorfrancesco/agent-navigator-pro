# Текущие задачи Agent Navigator Pro (v3.0)

## Выполнено сегодня [2026-03-03]
- [x] **Фикс: name 'asyncio' is not defined** в `document_analysis.py`.
- [x] **Верификация Map-Reduce:** Прогнали 12-страничное ТЗ, получили 10 КБ отчёт с ТТХ серверов.
- [x] **Стабилизация GPU:** Проверили нагрузку (nvidia-smi), 2x RTX 2070 держат LLM-инференс.
- [x] **Логирование:** Обновлён `Context_Log.md` с результатами анализа.

## В работе (Backlog / План)
- [ ] **Промпт-инжиниринг (Reduce):** Научить модель соотносить сроки и гарантии между разными чанками (устранение "не указано").
- [ ] **Расширение таблиц:** Увеличить лимит `specs` в Markdown-таблице (с 80 до 200 симв).
- [ ] **Параллельная суммаризация (Asyncio.Semaphore):** Ускорить обработку в 2-3 раза за счёт асинхронного конвейера.
    *   *Тех. обоснование:* Используем `asyncio.Semaphore(1)` для GPU-запросов. Логически запускаем всё через `gather`, но физически инференс идёт строго по одному. Это позволяет готовить следующий чанк (CPU/Disk), пока GPU занят текущим, не вызывая OOM на RTX 2070 (7.5/8GB VRAM).
- [ ] **Resource-Efficient Agentic RAG (Tier 4):** Реализовать последовательную итерацию (Нашёл -> Оценил -> Доискал) вместо параллельных агентов для экономии VRAM.
- [ ] **Vision Интеграция:** Подключить Qwen-VL в режиме "Single Instance" (с выгрузкой основной модели при необходимости).

## Критические исправления (Technical Debt)
- [ ] **Uptime:** Разобраться с `llama-server defunct` после простоя (периодический перезапуск или healthcheck-worker).
- [ ] **Cache:** Проверить эффективность prompt-cache при повторных запросах к одному документу.
