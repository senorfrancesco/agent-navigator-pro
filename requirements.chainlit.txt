# Chainlit UI — минимальные зависимости для Docker-контейнера.
# Тяжёлые библиотеки (torch, llama-cpp, onnx, sentence-transformers)
# работают на хосте; Chainlit обращается к ним через HTTP.

# UI framework
chainlit>=2.0.0

# Chat history persistence (SQLite)
aiosqlite>=0.20.0

# LangGraph workflows
langgraph>=0.6.0
langchain>=0.3.0

# HTTP clients (вызовы к хостовым сервисам)
httpx>=0.27.0
requests>=2.32.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.7.0

# RAG pipeline (retriever, classifier use numpy)
numpy>=1.26.0
